<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.alembrum"
      android:versionCode="83"
      android:versionName="2.63"
      android:launchMode = "singleInstance">
    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="33" />
	<!-- targetSdkVersion:12 for real but 26 to meet Google requirement-->
    <!-- Tell the system this app requires OpenGL ES 2.0. -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application android:icon="@mipmap/icon_foreground" android:label="The Land of Alembrume">
    
        <activity android:name=".ZildoActivity"
            	  android:screenOrientation="landscape"
            	  android:launchMode="singleTask"
                  android:theme="@style/Theme.Zildo"
                  android:label="The Land of Alembrume"
                  android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>